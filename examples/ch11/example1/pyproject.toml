[project]
name = "example1-tightly-coupled"
version = "0.1.0"
description = "Example 1: Tightly Coupled Flask Application"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "Flask>=2.0,<3.0",
    "pytest>=6.0",
]

[tool.pytest.ini_options]
pythonpath = "."
