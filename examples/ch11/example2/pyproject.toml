[project]
name = "example2-layered-architecture"
version = "0.1.0"
description = "Example 2: Layered Flask Application"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "Flask>=2.0,<3.0",
    "pytest>=6.0",
]

[tool.pytest.ini_options]
pythonpath = "."
